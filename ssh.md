**ssh-keygen** is a command-line utility used to create, manage, and convert authentication keys for SSH (Secure Shell). It generates:
- Private key (kept secret)
- Public key (shared with remote systems)
